# Release Commands

Create and push the `v0.1.0` tag:

```bash
git tag -a v0.1.0 -m "Initial public GhostKV research release"
git push origin v0.1.0
```

## Why releases matter

- releases make a research snapshot easier to reference
- tagged revisions improve citation stability
- versioned artifacts improve reproducibility when result files evolve over time

