# Results

This directory is reserved for experiment outputs, plots, and saved summaries.

No results are committed by default. The repository avoids fabricated benchmarks and expects users to generate results locally.

