# Data

This repository starts with synthetic tensors and does not require external datasets.

If later experiments add captured attention traces or benchmark inputs, place lightweight metadata or download instructions here rather than committing large binaries.

